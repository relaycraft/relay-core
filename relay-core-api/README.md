# relay-core-api

Internal support crate. Shared data contracts (Flow, Rule, Policy) used across RelayCore.

**Users should depend on `relay-core-runtime` instead** — its public API re-exports these types.

## License

MIT
